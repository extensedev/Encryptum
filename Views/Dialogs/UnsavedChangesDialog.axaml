<shad:Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Encryptum.ViewModels.Dialogs"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shad="clr-namespace:ShadUI;assembly=ShadUI"
        mc:Ignorable="d" d:DesignWidth="380"
        x:Class="Encryptum.Views.Dialogs.UnsavedChangesDialog"
        x:DataType="vm:UnsavedChangesViewModel"
        Title="Unsaved Changes"
        Width="380"
        SizeToContent="Height"
        CanResize="false"
        TitleBarAnimationEnabled="False"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/icon.ico">

    <shad:Window.LogoContent>
        <Image Source="/Assets/icon.png" Width="20" Height="20"/>
    </shad:Window.LogoContent>

    <Border Padding="24">
        <StackPanel Spacing="16">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <LucideIcon Kind="FileQuestionMark" Size="24" Foreground="{DynamicResource PrimaryColor}" VerticalAlignment="Center"/>
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="Unsaved Changes" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="{Binding Message}" FontSize="14" TextWrapping="Wrap"/>

            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button Command="{Binding CancelCommand}" Classes="Outline" Content="Cancel" Padding="16,6"/>
                <Button Command="{Binding DiscardCommand}" Classes="Outline" Content="Discard" Padding="16,6"/>
                <Button Command="{Binding SaveCommand}" Classes="Primary" Content="Save" Padding="16,6"/>
            </StackPanel>
        </StackPanel>
    </Border>

</shad:Window>